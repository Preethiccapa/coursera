
int find_minimum(unsigned char,unsigned char);

/**
 * @brief finds minimum value of an unsigned 32-bit char array
 * This function takes as an array of inputs 32-bit char.This will
 * finds the minimum value of the full array.
 *
 *@paramword To find minimum value from 32-bit array 
 *@return minimum value of 32-bit array
*/

int find_maximum(unsigned char,unsigned char);

/**
 * @brief finds maximum value of an unsigned 32-bit char array
 * This function takes as an array of inputs 32-bit char.This will
 * finds the maximum value of the full array.
 *@paramword To find maximum value from 32-bit array 
 *@return maximum value of 32-bit array
*/

double find_mean(unsigned char ,unsigned char );

/**
 * @brief finds mean value of an unsigned 32-bit char array
 * This function takes as an array of inputs 32-bit char.This will
 * finds the mean value of the full array.
 *
 *@paramword To find mean value from 32-bit array 
 *@return mean value of 32-bit array
*/

void swap(unsigned char *p,unsigned char *q);

/**
 * @brief Reverse the byte order of an unsigned 32-bit char array
 * This function takes as an input a 32-bit unsigned char array.This will
 * reverse the byte order of the full values.Instead of changing the 
 * paramter passed it, it will returned a reversed unsigned 32-bit 
 * values
 *
 *@paramword The 32-bit values to be reversed
 *@return The reversed 32-bit values
*/

void sort(unsigned char a[],unsigned char n);

/**
 * @brief sorts the byte order of an unsigned 32-bit values
 * This function takes as an input a 32-bit array.This will
 * sorts the byte order of the full values in an increasing order.
 * 
 *@paramword The 32-bit word to be sorted
 *@return The sorted 32-bit value array
*/

int median(unsigned char size1);

/**
 * @brief finds median value of an unsigned 32-bit char array
 * This function takes as an array of inputs 32-bit char.This will
 * finds the mean value of the full array.
 *
 *@paramword To find mean value from 32-bit array 
 *@return mean value of 32-bit array
*/
